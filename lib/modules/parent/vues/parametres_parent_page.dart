import 'package:flutter/material.dart';

class ParametresParentPage extends StatelessWidget {
  const ParametresParentPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Paramètres",
        style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold),
      ),
    );
  }
}
